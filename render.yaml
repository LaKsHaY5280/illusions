services:
  - type: web
    name: optical-illusions-game
    env: node
    buildCommand: pnpm install && pnpm build
    startCommand: NODE_ENV=production node server.mjs
    envVars:
      - key: NODE_VERSION
        value: 20
